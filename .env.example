# Copy this file to .env.local and fill in each value before running the app locally.
# The Gemini key should never be committed; leave it empty if you only need Supabase locally.
GEMINI_API_KEY=
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key
